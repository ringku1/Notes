<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIApp1.FindPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIApp1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Command Bar (Find, Up/Down Arrows, Replace options) -->
        <CommandBar VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="LightGray">
            <!-- Find section -->
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Find" Label="Find" Click="OnFindClick" />
                <AppBarButton Icon="Previous" Label="Previous" Click="OnFindPreviousClick" />
                <AppBarButton Icon="Next" Label="Next" Click="OnFindNextClick" />
            </CommandBar.PrimaryCommands>

            <!-- More options, like "Replace" functionality -->
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="More" Label="More" Click="OnMoreOptionsClick" />
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <!-- Search Field, Replace Field, and Replace/Replace All -->
        <StackPanel VerticalAlignment="Top" Margin="10, 50, 10, 10" Spacing="10">
            <!-- Find Text -->
            <TextBox x:Name="SearchBox" PlaceholderText="Search..." Width="300" Height="30" Margin="0, 0, 0, 10" />

            <!-- Replace Text Field -->
            <TextBox x:Name="ReplaceBox" PlaceholderText="Replace with..." Width="300" Height="30" Margin="0, 0, 0, 10" />

            <!-- Replace options dropdown -->
            <DropDownButton x:Name="ReplaceOptionsButton" Content="Replace Options">
                <DropDownButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <Button Content="Replace" Click="OnReplaceClick" />
                            <Button Content="Replace All" Click="OnReplaceAllClick" />
                        </StackPanel>
                    </Flyout>
                </DropDownButton.Flyout>
            </DropDownButton>

            <!-- Indicator for found text position (Up/Down arrows) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10" VerticalAlignment="Center">
                <Button Content="↑" Width="30" Height="30" Click="OnFindPreviousClick" />
                <Button Content="↓" Width="30" Height="30" Click="OnFindNextClick" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
